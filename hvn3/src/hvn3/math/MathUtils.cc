#include "hvn3/math/MathUtils.h"

#include <cmath>

namespace hvn3 {
	namespace Math {

		const long double Pi = std::acos(-1.0L);
		const float Epsilon<float>::value = 0.001f;
		const double Epsilon<double>::value = 0.001;

	}
}