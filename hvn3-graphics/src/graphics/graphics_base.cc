#include "graphics/graphics_base.h"

#include "graphics/pen.h"

namespace hvn3::graphics {

	//void GraphicsBase::DrawLine(float startX, float startY, float endX, float endY, const Color& color, float thickness) {

	//	IGraphics::DrawLine(math::Line2f(startX, startY, endX, endY), Pen(color, thickness));

	//}
	//void GraphicsBase::DrawLine(float startX, float startY, float endX, float endY, const IPen& pen) {

	//	IGraphics::DrawLine(math::Line2f(startX, startY, endX, endY), pen);

	//}
	//void GraphicsBase::DrawLine(const math::LineF& line, const Color& color, float thickness) {

	//	IGraphics::DrawLine(line, Pen(color, thickness));

	//}

	//void GraphicsBase::DrawRectangle(float x, float y, float width, float height, const Color& color, float thickness) {

	//	IGraphics::DrawRectangle(math::RectangleF(x, y, width, height), Pen(color, thickness));

	//}
	//void GraphicsBase::DrawRectangle(float x, float y, float width, float height, const IPen& pen) {

	//	IGraphics::DrawRectangle(math::RectangleF(x, y, width, height), pen);

	//}
	//void GraphicsBase::DrawRectangle(const math::RectangleF& rectangle, const Color& color, float thickness) {

	//	IGraphics::DrawRectangle(rectangle, Pen(color, thickness));

	//}

	//void GraphicsBase::FillRectangle(float x, float y, float width, float height, const Color& color) {

	//	IGraphics::FillRectangle(math::RectangleF(x, y, width, height), color);

	//}

	//void GraphicsBase::DrawRoundedRectangle(float x, float y, float width, float height, float radius, const Color& color, float thickness) {

	//	IGraphics::DrawRoundedRectangle(math::RectangleF(x, y, width, height), radius, Pen(color, thickness));

	//}
	//void GraphicsBase::DrawRoundedRectangle(float x, float y, float width, float height, float radius, const IPen& pen) {

	//	IGraphics::DrawRoundedRectangle(math::RectangleF(x, y, width, height), radius, pen);

	//}
	//void GraphicsBase::DrawRoundedRectangle(const math::RectangleF& rectangle, float radius, const Color& color, float thickness) {

	//	IGraphics::DrawRoundedRectangle(rectangle, radius, Pen(color, thickness));

	//}

	//void GraphicsBase::FillRoundedRectangle(float x, float y, float width, float height, float radius, const Color& color) {

	//	IGraphics::FillRoundedRectangle(math::RectangleF(x, y, width, height), radius, color);

	//}

}